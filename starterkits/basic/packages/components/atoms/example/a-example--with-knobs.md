# This is example modifier component description.

Plus to `a-example.md` description we may add any overrides/attaches in descriptions. For example this content comes from `a-example--with-knobs.md`
